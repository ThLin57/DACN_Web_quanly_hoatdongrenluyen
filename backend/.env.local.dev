# Local dev overrides for running node scripts directly (outside docker)
DATABASE_URL=postgresql://admin:abc@localhost:5432/Web_QuanLyDiemRenLuyen?schema=public
# Reuse same secret as compose (if needed for JWT tests)
JWT_BI_MAT=devsecretlocal
